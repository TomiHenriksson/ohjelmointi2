/*Write a program that prompts the user for numbers.
When the user enters one of the numbers he previously entered,
 the program will announce that the
 */

n = prompt("Enter number")
let number = parseInt(n)

while (number)